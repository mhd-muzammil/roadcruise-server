# Dependencies
node_modules

# Environment Variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Build Files
dist
build

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS Files
.DS_Store
Thumbs.db

# IDE
.vscode
.idea

# Vite
vite.svg

# Coverage
coverage

# Prisma
prisma/dev.db
prisma/dev.db-journal

# Uploads — runtime media storage only. Anchored to the repo root and to
# src/config so it never swallows the src/uploads/ SOURCE module (the multer
# middleware), which MUST be committed.
/uploads/
src/config/uploads/

# Cache
.cache

# Temporary Files
tmp
temp

# Generated Files
*.pid
*.seed
*.pid.lock

# Notification engine runtime data (JSON store — do not commit logs/PII)
src/notifications/data/

# Payment module runtime data (JSON store — do not commit transactions/PII)
src/payments/data/

# Auth module runtime data (sessions + audit trail — do not commit)
src/auth/data/

# SQLite database — the live runtime store. Contains real customer PII and
# password hashes; NEVER commit it, its WAL/SHM sidecars, backups, or the
# one-time migration backup of the legacy JSON store.
src/config/*.db
src/config/*.db-wal
src/config/*.db-shm
src/config/backups/
src/config/db.json
src/config/db.json.migrated.bak